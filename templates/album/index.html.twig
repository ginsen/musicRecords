{% extends 'base.html.twig' %}

{% block title %}Album List{% endblock %}

{% block body %}
    <h1>Music Record Dashboard</h1>

    <div>
        <h2>Album list</h2>
        <ul>
            {% for album in albums %}
                <li>
                    {{ album.title }}
                    [<a href="{{ path('album_show', { 'id': album.id }) }}">show album artist</a>]
                    [<a href="{{ path('album_remove', { 'id': album.id }) }}">Delete</a>]
                </li>
            {% endfor %}
        </ul>
        <a href="{{ path('album_new') }}">Add new Album</a>
    </div>

    <div>
        <h2>Artist company</h2>
        <ul>
            {% for artist in artists %}
                <li>
                    {{ artist.name }}
                    [<a href="{{ path('artist_show', { 'id': artist.id }) }}">show fed received by album</a>]
                </li>
            {% endfor %}
        </ul>
        <a href="{{ path('artist_new') }}">Add new Artist</a>
    </div>


{% endblock %}
